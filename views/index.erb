<h2>Welcome to Pizza Shop</h2>

<div class="row">
  <% @products.each do |p| %>
  <div class="col-md-3">

    <div>
      <%= p.title %>
    </div>

    <div>
      <img src="<%= p.path_to_image %>" alt="pizza" style="max-height: 150px;">
    </div>

    <div>
      <%= p.description %>
    </div>
    
    <div>
      <%= p.size %> сm
    </div>

    <div>
      <%= p.price %> Rub
    </div>
    
  </div>
<% end %>
</div>
<br>
<br>

<button class="btn btn-primary" onclick="something()">Click me</button>
